FROM python:3.12-alpine

RUN apk add --no-cache bash git docker-cli docker-cli-compose

WORKDIR /srv
COPY deploy/hetzner/deploy-hook-server.py /srv/server.py

EXPOSE 9000
CMD ["python", "/srv/server.py"]
